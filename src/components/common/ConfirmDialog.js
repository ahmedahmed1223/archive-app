import {
  appAlert as legacyAppAlert,
  appConfirm as legacyAppConfirm,
  appPrompt as legacyAppPrompt
} from "../legacyComponentAdapter.js";

export const appAlert = legacyAppAlert;
export const appConfirm = legacyAppConfirm;
export const appPrompt = legacyAppPrompt;
